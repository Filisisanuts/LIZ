{
  "name": "ax-coffee",
  "version": "1.0.0",
  "description": "咖啡/茶室经营管理系统",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit && node scripts/check-legacy-syntax.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/xlsx": "^0.0.35",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0",
    "xlsx": "^0.18.5"
  }
}
