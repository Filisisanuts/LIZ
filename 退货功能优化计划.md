# 退货功能优化计划

## 目标

1. **修复退货日期问题** - 退货物品应该使用选择的退货日期，而不是原采购日期
2. **增加手动退货功能** - 在采购手动录入页增加退货选项
3. **支持关联原采购记录** - 退货时可选择是否关联原采购（可选）
4. **优化退货显示** - 在采购页增加"退货"来源分组

---

## 一、修复退货日期问题

### 问题
当前 `_doReturnExec` 函数会把退货物品添加到原采购批次，忽略用户选择的退货日期。

### 修改文件
**purchase.js** (第1724-1770行)

### 修改内容
```javascript
// 修改前：添加到原批次
if (!target) {
    target = { date: retDate, source: orig.source, items: [] };
    db.purchases.push(target);
}
target.items.push({...});

// 修改后：始终创建新批次
var target = {
    date: retDate,  // 使用用户选择的退货日期
    source: '退货',  // 来源设为'退货'
    items: [{
        name: name,
        section: orig.section,
        category: orig.category,
        qty: -retQty,
        unit: orig.unit || '',
        unitPrice: orig.unitPrice || 0,
        total: -retTotal,
        source: '退货',
        note: retReason,
        relatedTo: { date: date, name: name }  // 新增：关联原采购信息
    }]
};
db.purchases.push(target);
```

---

## 二、采购手动录入页增加退货选项

### 修改文件
**purchase.js**

### 2.1 修改来源下拉框 (第274-276行)
```javascript
// 修改前
h += '<label>来源</label><select class="inp" id="pmSrc" style="max-width:120px">';
h += '<option>岸香贸易</option><option>外购</option></select>';

// 修改后：增加退货选项
h += '<label>来源</label><select class="inp" id="pmSrc" style="max-width:120px" onchange="pmSrcChanged()">';
h += '<option>岸香贸易</option><option>外购</option><option value="退货">退货</option></select>';
```

### 2.2 新增来源变化处理函数
```javascript
// 当来源选择"退货"时，显示关联原采购字段
function pmSrcChanged() {
    var src = $id('pmSrc').value;
    var relDiv = $id('pmRelDiv');
    if (relDiv) {
        relDiv.style.display = src === '退货' ? '' : 'none';
    }
}

// 渲染关联原采购下拉框
function renderPmRelSelect() {
    var items = [];
    DB.purchases.forEach(function(p) {
        (p.items || []).forEach(function(item) {
            if (item.qty > 0 && item.source !== '退货') {
                items.push({
                    date: p.date,
                    name: item.name,
                    unitPrice: item.unitPrice,
                    total: item.total,
                    source: p.source || '外购'
                });
            }
        });
    });

    var h = '<option value="">不关联原采购</option>';
    items.forEach(function(item, idx) {
        h += '<option value="' + idx + '">' + item.name + ' - ' + item.date + ' - ¥' + fmtC(item.total) + '</option>';
    });
    return h;
}
```

### 2.3 修改手动录入表单 (在第293行后插入)
```javascript
// 在区域/分类行之后，品名行之前，插入关联原采购字段
h += '<div class="hrow" id="pmRelDiv" style="display:none">';
h += '<label>关联原采购</label>';
h += '<select class="inp" id="pmRelPur" style="flex:1" onchange="pmRelPurChanged()">';
h += '<option value="">不关联原采购</option>';
h += '</select>';
h += '</div>';
```

### 2.4 新增关联选择变化处理函数
```javascript
// 选中关联原采购后，自动填充品名、单价等信息
function pmRelPurChanged() {
    var sel = $id('pmRelPur');
    if (!sel || !sel.value) return;

    var items = getAllReturnableItems();
    var item = items[parseInt(sel.value)];
    if (!item) return;

    $id('pmName').value = item.name;
    $id('pmUnitPrice').value = item.unitPrice;
    // 触发计算
    calcTotal();
}

// 获取所有可退货的采购物品
function getAllReturnableItems() {
    var items = [];
    DB.purchases.forEach(function(p) {
        (p.items || []).forEach(function(item) {
            if (item.qty > 0 && item.source !== '退货') {
                items.push({
                    date: p.date,
                    name: item.name,
                    unitPrice: item.unitPrice || 0,
                    total: item.total || 0,
                    source: p.source || '外购'
                });
            }
        });
    });
    return items;
}
```

---

## 三、修改保存逻辑

### 修改文件
**purchase.js** (第778-805行)

### 修改 `saveMPur` 函数
```javascript
function saveMPur() {
    var date = $id('pmDate').value || td();
    var src = $id('pmSrc').value || '外购';
    var relIdx = $id('pmRelPur') ? $id('pmRelPur').value : '';

    // 如果选择了关联原采购，获取关联信息
    var relatedTo = null;
    if (relIdx && src === '退货') {
        var relItems = getAllReturnableItems();
        var relItem = relItems[parseInt(relIdx)];
        if (relItem) {
            relatedTo = { date: relItem.date, name: relItem.name };
        }
    }

    // ... 原有的物品验证逻辑 ...

    var items = _pmItems.map(function(it) {
        return {
            name: it.name,
            section: it.section,
            category: it.category,
            qty: src === '退货' ? -Math.abs(it.qty) : it.qty,  // 退货时数量取负
            unit: it.unit,
            unitPrice: it.unitPrice,
            total: src === '退货' ? -Math.abs(it.total) : it.total,  // 退货时金额取负
            source: src,
            relatedTo: relatedTo  // 新增：关联信息
        };
    });

    // ... 后续保存逻辑不变 ...
}
```

---

## 四、优化从采购记录退货的弹窗

### 修改文件
**purchase.js** (第1629-1646行)

### 修改退货表单，增加"取消关联"复选框
```javascript
function returnPurItem(date, name) {
    // ... 原有逻辑 ...

    var h = '<h3>退货 · ' + name + '</h3>';
    h += '<div class="pv-row"><span class="k">来源</span><span>' + (foundP.source || '外购') + '</span></div>';
    h += '<div class="pv-row"><span class="k">原采购</span><span>' + fmtC(orig.total) + '</span></div>';
    h += '<div class="pv-row"><span class="k">数量</span><span>' + orig.qty + (orig.unit || '') + '</span></div>';
    h += '<div class="pv-row"><span class="k">单价</span><span>' + fmtC(orig.unitPrice || 0) + '</span></div>';

    h += '<div class="section-label">退货信息</div>';
    h += '<div class="hrow"><label>退货日期</label><input class="inp" id="retDate" type="text" readonly placeholder="选择日期" value="' + td() + '" onclick="_dpOpen(\'retDate\')" style="max-width:150px;cursor:pointer"></div>';
    h += '<div class="hrow"><label>退货数量</label><input class="inp" id="retQty" type="number" step="0.01" value="' + orig.qty + '" style="max-width:100px"></div>';
    h += '<div class="hrow"><label>退货金额</label><input class="inp" id="retTotal" type="number" step="0.01" value="' + orig.total + '" style="max-width:120px"></div>';
    h += '<div class="hrow"><label>退货原因</label><input class="inp" id="retReason" placeholder="可选" style="flex:1"></div>';

    // 新增：关联原采购复选框
    h += '<div class="hrow"><label></label><label style="display:flex;align-items:center;gap:6px;cursor:pointer">';
    h += '<input type="checkbox" id="retRelate" checked style="width:16px;height:16px">';
    h += '<span>关联原采购记录</span></label></div>';

    h += '<div class="brow" style="margin-top:12px;justify-content:flex-end">';
    h += '<button class="btn p" onclick="doReturnPurItem(\'' + date + '\',' + sq(name) + ')">确认退货</button>';
    h += '<button class="btn" onclick="backToPurDetail(\'' + date + '\')">取消</button></div>';

    showModal(h, 500);
}
```

### 修改 `doReturnPurItem` 函数，读取关联选项
```javascript
function doReturnPurItem(date, name) {
    var retDate = $id('retDate').value || td();
    var retQty = parseFloat($id('retQty').value) || 0;
    var retTotal = parseFloat($id('retTotal').value) || 0;
    var retReason = $id('retReason').value.trim();
    var retRelate = $id('retRelate') ? $id('retRelate').checked : true;  // 新增：读取关联选项

    // ... 后续逻辑中传递 retRelate 参数 ...
    // 在 _doReturnExec 中根据 retRelate 决定是否设置 relatedTo 字段
}
```

---

## 五、采购页显示优化

### 修改文件
**purchase.js** (第1100-1200行左右)

### 修改 `renderPHist` 函数，增加"退货"分组
```javascript
// 在渲染各来源分组时，增加"退货"分组
function renderPHistMonth(purchases) {
    // ... 原有逻辑 ...

    // 按来源分组
    var grouped = {};
    purchases.forEach(function(p) {
        var src = p.source || '外购';
        if (!grouped[src]) grouped[src] = [];
        grouped[src].push(p);
    });

    // 确保"退货"分组存在
    var sources = Object.keys(grouped);
    if (!sources.includes('退货')) {
        sources.push('退货');
    }

    // 按顺序显示：外购、岸香贸易、...、退货
    var order = ['外购', '岸香贸易', '美团', '饿了么', '退货'];
    sources.sort(function(a, b) {
        var ia = order.indexOf(a);
        var ib = order.indexOf(b);
        if (ia === -1) ia = 999;
        if (ib === -1) ib = 999;
        return ia - ib;
    });

    // 渲染各分组
    sources.forEach(function(src) {
        var items = grouped[src] || [];
        // ... 渲染该分组的物品列表 ...
    });
}
```

---

## 六、涉及的文件清单

| 文件 | 修改内容 |
|------|----------|
| purchase.js | 1. 修复退货日期问题<br>2. 来源下拉增加"退货"<br>3. 增加关联原采购字段<br>4. 修改保存逻辑支持负数<br>5. 退货弹窗增加关联复选框<br>6. 采购页增加退货分组 |

---

## 七、测试要点

1. **修复退货日期**
   - 从采购记录退货，选择不同日期
   - 验证退货记录创建在选择的日期下

2. **手动退货功能**
   - 在手动录入页选择来源"退货"
   - 录入物品，验证数量/金额为负数
   - 保存后验证显示在"退货"分组

3. **关联原采购功能**
   - 手动退货时选择关联原采购
   - 验证自动填充品名、单价
   - 不选择关联时，验证创建独立退货记录

4. **从采购记录退货**
   - 验证"关联原采购"复选框默认勾选
   - 取消勾选后，验证创建独立退货记录

5. **采购页显示**
   - 验证"退货"分组正确显示
   - 验证退货物品数量/金额为负数
   - 验证关联原采购的退货显示关联信息

---

## 八、预计工时

- 修复退货日期：10分钟
- 手动退货功能：30分钟
- 关联原采购功能：20分钟
- 退货弹窗优化：10分钟
- 采购页显示优化：20分钟
- 测试验证：20分钟

**总计：约 1.5 小时**

---

## 九、风险点

1. **数据兼容性** - 新增的 `relatedTo` 字段对旧数据无影响
2. **UI布局** - 增加关联字段可能影响移动端显示，需要测试
3. **负数处理** - 确保库存计算、报表统计正确处理负数数量

---

**栗子，这个计划可以吗？确认后我开始实现。**
